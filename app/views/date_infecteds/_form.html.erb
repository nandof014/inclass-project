<%= form_with(model: date_infected, local: true) do |form| %>
  <% if date_infected.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(date_infected.errors.count, "error") %> prohibited this date_infected from being saved:</h2>

      <ul>
        <% date_infected.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date_infected %>
    <%= form.text_field :date_infected %>
  </div>

  <div class="field">
    <%= form.label :animal_id %>
    <%= form.collection_select :animal_id, Animal.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :disease_id %>
    <%= form.collection_select :disease_id, Disease.all, :id, :description  %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
